# ملخص التصحيحات

## 1) الثقب الأمني الأساسي — `release-escrow` (supabase/functions/release-escrow/index.ts)

**المشكلة:** الدالة كانت تاخذ `amount`, `item_title`, `star_name`, `fan_email`
مباشرة من الطلب الجاي من المتصفح، وتحسب عليها التقسيم 90/10. أي شخص يعرف
`escrow_id` و `payment_intent_id` يقدر يستدعي الدالة بـ `amount` مزيف، فتُحصّل
Stripe المبلغ الحقيقي المحجوز لكن حساب حصة النجم/العمولة يصير على رقم آخر.

**التصحيح:**
- الدالة الآن تجيب `amount`/`item_title`/`star_name`/`fan_email` **من صف
  `escrow_transactions` في القاعدة نفسها**، ماشي من الطلب. الطلب ما يأثرش فحساب
  الفلوس نهائيًا.
- إضافة حارس حالة: لا يمكن تحرير escrow إلا لو كانت حالته `held` (يمنع تحرير
  نفس الصف مرتين — double-capture).
- **فحص إضافي مهم:** بعد ما Stripe يحصّل المبلغ فعليًا، الدالة تتحقق أن المبلغ
  الذي حصّلته Stripe فعلاً (`amount_received`) يطابق `amount` المخزّن في الصف.
  لو ما تطابقا (مثلاً صف escrow مزيف يشير لـ payment intent بمبلغ مختلف)، يتم
  إلغاء العملية، وتعليم الصف بحالة `failed`، وما يتسجلش أي دفعة أو bid أو إيميل.
- حماية من race condition عند التحديث (`.eq("status","held")` وقت الـ update).

## 2) تسريب بريد المستخدمين عبر لوحة Insights العلنية

**المشكلة:** جدول `bids` كان يخزن إيميل المستخدم الحقيقي (`fan`)، ويعرضه
`analytics-widget.js` علنيًا لأي زائر ("won by فلان@ايميل.كوم").

**التصحيح:** `release-escrow` الآن يخزن نسخة مقنّعة من الإيميل فقط
(مثلاً `j***@example.com`) عبر دالة `maskEmail()`، قبل الإدراج في `bids`. لا
حاجة لتغيير `analytics-widget.js` — يعرض القيمة كما هي، وهي الآن آمنة للعرض
العلني.

## 3) تحديث `escrow-widget.js`

الطلب لـ `release-escrow` صار يرسل غير `escrow_id` فقط — إزالة الحقول الأخرى
لأن السيرفر ما عاد يقرأها، وإرسالها كان يعطي انطباع خاطئ أن المتصفح يتحكم
بحساب الفلوس.

## 4) تشديد RLS على `escrow_transactions` (schema.sql)

فحوصات أساسية إضافية عند الإدراج (`amount > 0`, `stripe_payment_intent_id`
غير فارغ). هذا مجرد فلتر أولي بسيط — الحماية الحقيقية هي فحص المبلغ الفعلي
المحصّل من Stripe في نقطة 1 أعلاه، لأن RLS ما يقدر يتحقق من Stripe مباشرة.

---

## ⚠️ ما لم يتم تصحيحه (يحتاج قرار منك)

- **لا يوجد نظام مستخدمين/تسجيل دخول حقيقي حاليًا.** بالتالي `release-escrow`
  ما يقدرش يتحقق "هل المستدعي هو فعلاً صاحب هذا escrow؟" — الفحوصات المضافة
  تمنع تزوير *المبلغ*، لكن أي شخص يعرف `escrow_id` يقدر يستدعي الإطلاق (لو
  الحالة `held` وتطابق المبلغ محصّل بشكل صحيح، هذا مقبول تقنيًا، لكن لو تحب
  تقييد "فقط الفان أو النجم المعنيين"، هذا يحتاج نظام Auth حقيقي — Supabase
  Auth أبسط طريق).
- **لا يوجد محرك مزايدة حي حقيقي** (auctions, current_highest_bid,
  anti-snipe) — الموجود حاليًا تدفق "شراء بسعر ثابت عبر escrow"، والمزايدة
  التنافسية موجودة فقط في `platform-simulation-widget.js` كعرض توضيحي وهمي.
- **الدفع للنجم غير مؤتمت فعليًا** — `star_payout`/`platform_fee` أرقام
  تُحسب وتُخزّن، لكن الفلوس الحقيقية تبقى في حساب Stripe الخاص بالمنصة حتى
  تدمج Stripe Connect أو تحوّل يدويًا.

هذي الثلاث نقاط ماشي "أخطاء" بمعنى ثغرات — هي ميزات لم تُبنى بعد، وتحتاج قرار
منك على أي واحدة تبني أول.
